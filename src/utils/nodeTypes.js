import { customBidirectionalNode, customSourceNode, customTargetNode } from "../CustomNodes";

export const nodeTypes = {
	source: customSourceNode,
	target: customTargetNode,
	bidirectional: customBidirectionalNode,
};
